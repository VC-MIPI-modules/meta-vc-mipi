# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have recipes-* directories, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "meta-vc-mipi-test"
BBFILE_PATTERN_meta-vc-mipi-test = "^${LAYERDIR}/"
BBFILE_PRIORITY_meta-vc-mipi-test = "6"

LAYERDEPENDS_meta-vc-mipi-test = "core"
LAYERSERIES_COMPAT_meta-vc-mipi-test = "kirkstone scarthgap walnascar"